{
  "name": "chrome-extension-repowiki",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "release": "git pull && npm version patch && git push && git push --tags",
    "release:minor": "git pull && npm version minor && git push && git push --tags",
    "release:major": "git pull && npm version major && git push && git push --tags"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wxt": "^0.20.20"
  }
}
